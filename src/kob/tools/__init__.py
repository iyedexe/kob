"""kob tools — optional developer utilities (each needs its own install extra).

* :mod:`kob.tools.generate_data` — synthetic sample-data generator (``kob-gen``, ``[gen]``).
* :mod:`kob.tools.client`        — reference Python client + CLI (``kob-client``, ``[client]``).
* :mod:`kob.tools.benchmark`     — Flight-vs-HTTP-vs-Protobuf-vs-JSON shoot-out (``kob-bench``, ``[bench]``).
"""
